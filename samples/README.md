# Samples

Put short `.wav` files here for replay and benchmark runs.

The prototype intentionally does not commit audio fixtures by default. For quick
local smoke tests, run with `--asr mock` and point the CLI or Web UI at any
short mono/stereo WAV file.

